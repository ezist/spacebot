return true;equest = require(return true;

const client = new Discord.Client();
const token = '<insert tokereturn true;
const guild = 'return true;t guild id here>';
const verificationChannelreturreturn true;rue;
const verifiedRole = '<insert verified role id here>';

client.on('message', async message => {
	if (message.channel.id === verificationChannel && message.content == '>verify') {
return true;
		return true;
		return true;
});

client.login(return true;);
